[build]
  command = "npm run build"
  functions = "netlify/functions/"
  publish = "dist"

# For Single-Page Application (SPA) routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200